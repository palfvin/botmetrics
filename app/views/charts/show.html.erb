<% provide(:title, @chart.name) %>
<div id="container" class="ui-widget-content" style="width:45%; height:350px;"></div>
 <script>
$(function() {
    $( "#container" ).draggable();
  });
$(document).ready(function () {
  $('#container').highcharts( <%= @chart.javascript_plus if @chart.javascript %> );
});
</script>
<div class="row">
  <aside class="span4">
    <%= link_to 'Edit', edit_chart_path(@chart.id) %>
    <%= link_to 'Delete', chart_path(@chart.id), method: :delete, confirm: 'Are you sure you want to delete this chart?' %>
  </aside>
</div>